# Memory

Persistent high-priority notes for this instruction root. The runtime may inject a truncated snapshot each turn, so keep this file short and curated.

## Rules

- Keep durable rules, critical user preferences, and compact project facts here.
- Prefer short bullets over prose.
- Remove stale or duplicated entries when adding better summaries.
- Keep this file within the runtime `MEMORY.md` budget; move longer facts into `memory/`.

## Notes

- Add the most important stable memory here.
