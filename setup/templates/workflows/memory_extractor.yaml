workflow_spec_version: 1
id: memory_extractor.turn
description: Sub-agent workflow for memory_extractor (runs under subs/); override .agent/workflows/memory_extractor.yaml.
steps:
  - use: on_receive
  # Extension hook — actual prompt merge is at adk_main (RenderMainAgentPrompt).
  - use: load_prompt_md
  # - use: load_memory_snapshot
  - id: adk_main
    use: adk_main
  - id: on_respond
    use: on_respond
